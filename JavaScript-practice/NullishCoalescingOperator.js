let userName = null ?? 'Default Name';  // 'Default Name' because name is null
let age = 25 ?? 30;                 // 25 because age is not null or undefined
console.log(userName, age); // Output: 'Default Name', 25
